You are improving the {app_name} app located at {app_path}.
App type: {app_type}

TASK: Add a small, useful feature that enhances the app's value.

STEPS:
1. Understand the app's purpose and current feature set:
   - Read the README, main pages, and key components
   - Identify the app type and primary user workflow
   - Note what features already exist

2. Choose ONE small feature appropriate for the app type:

   For study/education apps:
   - Quiz mode with flashcard-style review
   - Progress tracking with simple statistics
   - Spaced repetition scheduling

   For AI/tool apps:
   - Conversation/result history with local storage
   - Export results to clipboard or file
   - Keyboard shortcuts for common actions

   For portfolio/blog apps:
   - Reading time estimate on articles
   - Table of contents for long pages
   - Copy code button on code blocks

   For productivity apps:
   - Keyboard shortcuts panel (Cmd+K or ?)
   - Recently viewed items
   - Quick search/filter functionality

   For any app:
   - Dark/light theme toggle (if not present)
   - Responsive mobile navigation
   - Share button with native Web Share API

3. Implement the feature:
   - Keep the scope small (1-3 files changed, under 200 lines added)
   - Follow existing code patterns and conventions
   - Use existing dependencies where possible
   - Ensure the feature works on mobile and desktop

4. Test the feature manually by reviewing the code flow.

CONSTRAINTS:
- Make minimal, focused changes. Don't break existing functionality.
- Add only ONE feature per improvement cycle.
- Don't add features that require new backend infrastructure or databases.
- Don't add features that require new API keys or external services.
- Keep the feature self-contained and easy to remove if needed.
- Follow the app's existing design patterns and styling.
- The feature should be discoverable and intuitive without documentation.

VERIFICATION:
- After making changes, verify by running: {build_command}
- Walk through the feature's user flow to confirm it works end-to-end.
